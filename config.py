gitlab_url = '<https://your.gitlab.url>'
gitlab_api_token = '<your api token>'

github_user = '<your user>'
github_api_token = '<your key>'

# Used in case usernames for gitlab and github are different
# The same username is used, if not specified here
usernames_map = {
    'gfuhr': 'gustavofuhr',
    'filipe': 'filipespeck',
    'bravo': None,
}
